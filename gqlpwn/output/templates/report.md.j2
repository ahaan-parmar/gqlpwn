# gqlpwn Security Report

| Field | Value |
|-------|-------|
| **Target** | `{{ result.target }}` |
| **Scan ID** | `{{ result.scan_id }}` |
| **Started** | {{ result.started_at | fmt_date }} |
| **Finished** | {{ result.finished_at | fmt_date }} |
| **Introspection** | {{ "Enabled" if result.introspection_enabled else "Disabled" }} |
| **Modules** | {{ result.modules_run | join(", ") }} |
| **Total Findings** | {{ result.findings | length }} |

---

## Finding Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | {{ by_severity.critical | length }} |
| 🟠 High     | {{ by_severity.high | length }} |
| 🟡 Medium   | {{ by_severity.medium | length }} |
| 🔵 Low      | {{ by_severity.low | length }} |
| ⚪ Info     | {{ by_severity.info | length }} |

---

{% for severity in ["critical", "high", "medium", "low", "info"] %}
{% if by_severity[severity] %}
## {{ severity | upper }} Findings

{% for finding in by_severity[severity] %}
### {{ loop.index }}. {{ finding.title }}

| | |
|-|-|
| **Module** | `{{ finding.module }}` |
| **Severity** | `{{ finding.severity | upper }}` (CVSS {{ "%.1f" | format(finding.cvss_score) }}) |
| **Endpoint** | `{{ finding.endpoint }}` |
| **Timestamp** | {{ finding.timestamp | fmt_date }} |

**Description**

{{ finding.description }}

**Evidence**

```
{{ finding.evidence }}
```

{% if finding.payload %}
**Payload**

```
{{ finding.payload }}
```
{% endif %}

**Remediation**

{{ finding.remediation }}

{% if finding.references %}
**References**

{% for ref in finding.references %}
- {{ ref }}
{% endfor %}
{% endif %}

---
{% endfor %}
{% endif %}
{% endfor %}

*Report generated by [gqlpwn](https://github.com/ahaan-parmar/gqlpwn) on {{ generated_at | fmt_date }}*
